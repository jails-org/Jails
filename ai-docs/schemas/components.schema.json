{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "JailsComponentModule",
  "type": "object",
  "required": ["default"],
  "properties": {
    "default": {
      "description": "Component controller function receiving Jails helpers.",
      "type": "string"
    },
    "model": {
      "description": "Initial state object or function receiving elm, initialState, and dependencies.",
      "oneOf": [{ "type": "object" }, { "type": "string" }]
    },
    "view": {
      "description": "Pure function that derives render-only values from state.",
      "type": "string"
    },
    "template": {
      "description": "Function that returns embedded component HTML.",
      "type": "string"
    },
    "registeredName": {
      "description": "Custom element name used with register().",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)+$"
    }
  },
  "additionalProperties": true
}

