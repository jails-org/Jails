# Directive Reference

Canonical machine-readable directive data lives in `directives.json`.

Directive expressions are JavaScript expressions evaluated against the component render state. They should be deterministic and side-effect free.

